id: initial_realm
author: Dominik Horn
changes:
  - addRealm:
      name: ${SSO_REALM}
  - updateRealm:
      id: ${SSO_REALM}
      displayName: Local Realm

  - addGroup:
      realm: ${SSO_REALM}
      name: group1